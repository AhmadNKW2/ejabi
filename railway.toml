[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
startCommand = "node apps/api/dist/main.js"
releaseCommand = "pnpm --filter @ejabi/api exec prisma migrate deploy"
